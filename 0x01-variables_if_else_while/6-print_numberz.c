#include <stdio.h>
/**
 * main - Entry poitn
 * Description: 'prints single numbers in base ten'
 * Return: always 0 (success)
 */
int main(void)
{
	int n = 0;

	for
	(n = 0;
	 n <= 9;
	 n++)
	{
		putchar(n + 48);
	}
	putchar('\n');
	return (0);
}
