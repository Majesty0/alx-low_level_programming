#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry poitn
 * Description: 'prints single numbers in base ten'
 * Return: always 0 (success)
 */
int main(void)
{
	int n = 0;

	while
	(n <= 9)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
