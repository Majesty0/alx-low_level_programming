#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints single whole numbers in base ten'
 * Return: always 0 (success)
 */
int main(void)
{
	int n = 0;

	while
	(n <= 9)
	{
		printf("%d\n", n);
		n++;
	}
	return (0);
}
